# [Created by task 3.4.1 5/14/2025 07:26:56]
data.location=~/.local/share/task
news.version=3.4.1

# To use the default location of the XDG directories,
# move this configuration file from ~/.taskrc to ~/.config/task/taskrc and update location config as follows:

#data.location=~/.local/share/task
#hooks.location=~/.config/task/hooks

# Color theme (uncomment one to use)
#include light-16.theme
#include light-256.theme
#include bubblegum-256.theme
include dark-16.theme
#include dark-256.theme
#include dark-red-256.theme
#include dark-green-256.theme
#include dark-blue-256.theme
#include dark-violets-256.theme
#include dark-yellow-green.theme
#include dark-gray-256.theme
#include dark-gray-blue-256.theme
#include solarized-dark-256.theme
#include solarized-light-256.theme
#include no-color.theme

# color overrides
color.active=green
# color.active=green inverse
color.alternate=on gray2
color.label=bold

# customize default report "next"; use task show next to see options
report.next.columns=id,depends,priority,project,tags,recur,scheduled.countdown,due.relative,until.remaining,description.count,urgency
report.next.labels=id,deps,prio,proj,tag,recur,sched,due,until,desc,urg
# specifying "/" (e.g., project+/) will add empty line between project sections
report.next.sort=start-,project+,urgency-

report.active.columns=id,depends,project,tags,recur,scheduled.remaining,due,until,description
report.active.labels=id,deps,proj,tag,recur,sched,due,until,desc
report.active.sort=project+,urgency-
